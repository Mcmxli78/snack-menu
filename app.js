const buttons=[...document.querySelectorAll('.category')],sections=[...document.querySelectorAll('.menu-view')],viewName=document.querySelector('#view-name'),labels={all:'All menu',food:'Food',beverages:'Beverages',shisha:'Shisha'};
function showCategory(filter,scroll=false){if(!labels[filter])filter='all';buttons.forEach(button=>{const selected=button.dataset.filter===filter;button.classList.toggle('active',selected);button.setAttribute('aria-pressed',String(selected))});sections.forEach(section=>{const visible=filter==='all'||section.dataset.section===filter;section.hidden=!visible;section.classList.toggle('active',visible)});viewName.textContent=labels[filter];history.replaceState(null,'',`#${filter}`);if(scroll)document.querySelector('.view-title').scrollIntoView({behavior:'smooth',block:'start'})}
buttons.forEach(button=>button.addEventListener('click',()=>showCategory(button.dataset.filter,true)));showCategory(location.hash.slice(1)||'all');document.querySelector('#year').textContent=new Date().getFullYear();
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.animate([{opacity:0,transform:'translateY(18px)'},{opacity:1,transform:'none'}],{duration:500,easing:'ease-out',fill:'both'});observer.unobserve(entry.target)}}),{threshold:.08});document.querySelectorAll('.menu-panel').forEach(panel=>observer.observe(panel));
